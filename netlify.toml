[build]
  base = '/web'
  publish = "/web/public"
  command = "yarn netlify-build"

[dev]
  functions = "/web/netlify-functions"
  publish = "/web/public"
  command = "yarn netlify-build"
  port = 8000
