[build]
  base = '/web'
  publish = "/web/public"
  command = "npm i yarn && yarn && yarn build"
  functions = "/web/netlify-functions"

[dev]
  functions = "/web/netlify-functions"
  publish = "/web/public"
  command = "yarn netlify-build"
  port = 8000
